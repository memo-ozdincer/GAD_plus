# Published NeuralNEB PaiNN Transition1x model. The checkpoint is held in
# scratch; the adapter derives conservative forces and a full autograd Hessian.
name: neuralneb
device: cuda
checkpoint: ${cluster.scratch_dir}/gadplus/models/neuralneb-t1x-8d9598b/models/painn0.sd
cutoff: 5.0
h5_path: ${cluster.project_dir}/data/transition1x.h5
